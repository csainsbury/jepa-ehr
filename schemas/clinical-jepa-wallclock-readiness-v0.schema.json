{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "clinical-jepa-wallclock-readiness-v0",
  "type": "object",
  "required": ["schema_version", "created_utc", "split", "horizons_days", "per_source", "aggregate_only"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["clinical-jepa-wallclock-readiness-v0"]},
    "created_utc": {"type": "string"},
    "split": {"type": "string"},
    "horizons_days": {"type": "array", "items": {"type": "number"}},
    "floor": {"type": "integer", "minimum": 0},
    "source_prefix_len": {"type": "integer", "minimum": 0},
    "per_source": {"type": "object"},
    "adequate_horizons_by_source": {"type": "object"},
    "aggregate_only": {"type": "boolean"},
    "notes": {"type": "string"}
  }
}
